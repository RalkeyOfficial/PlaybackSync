{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "BUFFER_END Message Schema",
  "description": "Schema for BUFFER_END message sent by clients when buffering ends and playback can resume",
  "required": ["type", "videoPos"],
  "properties": {
    "type": {
      "type": "string",
      "const": "BUFFER_END"
    },
    "videoPos": {
      "type": "number",
      "minimum": 0,
      "description": "Current playback position when buffering ended (seconds)"
    }
  },
  "additionalProperties": false
}
