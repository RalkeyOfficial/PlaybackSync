{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "HEARTBEAT Message Schema",
  "description": "Schema for HEARTBEAT message sent by clients for regular status updates and drift detection",
  "required": ["type", "currentPos", "playerState"],
  "properties": {
    "type": {
      "type": "string",
      "const": "HEARTBEAT"
    },
    "currentPos": {
      "type": "number",
      "minimum": 0,
      "description": "Current playback position reported by client (seconds)"
    },
    "playerState": {
      "type": "string",
      "enum": ["playing", "paused", "buffering"],
      "description": "Current player state"
    },
    "clockSample": {
      "type": "number",
      "description": "Optional clock sample for clock synchronization (client timestamp)"
    }
  },
  "additionalProperties": false
}
