# Development Dockerfile with hot reloading
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm install

# Copy source files
COPY . .

# Expose port
EXPOSE 8080

# Use nodemon with tsx for hot reloading
CMD ["npm", "run", "dev:watch"]
