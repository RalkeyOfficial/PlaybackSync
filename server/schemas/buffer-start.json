{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "BUFFER_START Message Schema",
  "description": "Schema for BUFFER_START message sent by clients when playback stalls and buffering begins",
  "required": ["type", "videoPos"],
  "properties": {
    "type": {
      "type": "string",
      "const": "BUFFER_START"
    },
    "videoPos": {
      "type": "number",
      "minimum": 0,
      "description": "Current playback position when buffering started (seconds)"
    }
  },
  "additionalProperties": false
}
