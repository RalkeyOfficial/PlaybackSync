{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "CLOCK_PING Message Schema",
  "description": "Schema for CLOCK_PING message sent by clients for clock synchronization (NTP-style). Used to calculate per-client clock offset and RTT.",
  "required": ["type", "clientSendTime"],
  "properties": {
    "type": {
      "type": "string",
      "const": "CLOCK_PING"
    },
    "clientSendTime": {
      "type": "number",
      "minimum": 0,
      "description": "Client timestamp when ping was sent (epoch ms)"
    }
  },
  "additionalProperties": false
}
